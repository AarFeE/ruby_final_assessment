<%= form_with(model: uploaded_image) do |form| %>
  <% if uploaded_image.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(uploaded_image.errors.count, "error") %> prohibited this uploaded_image from being saved:</h2>

      <ul>
        <% uploaded_image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
